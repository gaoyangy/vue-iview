# vue-iview

> A Vue.js project

## Build Setup

``` bash
# install dependencies
npm install

#模拟数据库启动 localhost:3000
npm run mockdev

# serve with hot reload at localhost:8080
npm run dev
到此可以启动

#git
git add --all
git commit -m ''
git push

remove 
git checkout
rm -r xxxx
git add --all
git commit -m ''
git push


#接口
所有api接口默认以/api开头

#模拟数据

普通模拟数据可以直接在/mock/db.json里面写

较大的数据可以在faker-data.js自动生成大量随机数据

可以参考sign.vue

#sass
关于sass的使用可以直接参考App.vue